# Скопируйте в ai-tester.config.yaml и поправьте под проект.
# Секреты сюда не кладите — этот файл в git. Ключ: переменная OPENAI_API_KEY
# или поле llm.api_key только в локальном ai-tester.config.yaml (он в .gitignore).

llm:
  base_url: "https://api.openai.com/v1"
  model: "gpt-4.1-mini"
  api_key_env: "OPENAI_API_KEY"
  # api_key: null  # опционально в ai-tester.config.yaml: строка ключа, если не хотите env
  temperature: 0.2
  # max_output_tokens: 16384  # лимит ответа (JSON с тест-кейсами)
  # max_requirement_chars_per_source: 52000  # усечение текста одного PDF/файла в промпте
  # max_consistency_findings: 40  # сколько finding’ов из отчёта отдавать в LLM

docs_dir: "tests/ai-docs"
sessions_dir: "tests/ai-sessions"

# Разрешённые окружения: host из --target-url должен совпадать с host у одного из base_url.
# Пустой список = проверка отключена, любой target_url.
envs: []
# envs:
#   - name: "qa-routes"
#     base_url: "https://qa-routes.praktikum-services.ru"
